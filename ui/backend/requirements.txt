fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
aiosqlite>=0.20.0
beautifulsoup4>=4.12.0
websockets>=13.0
# crawl4ai is optional — requires playwright + MSVC on Windows (Python 3.14 has no wheel).
# Install manually if you want the richer scraper: pip install crawl4ai
