# Issues — git-suite (GitHub Index Builder)

## Open

_Pipeline gap — the built app implements "absorb = repo→hub"; the intended model is "absorb = features→owned repo". Steps 3, 5, 6, 7, 8 of the [architecture model](ui/ROADMAP.md#architecture-model--directed-grouping--analysis--promotion--planning) are unbuilt. See [[git-suite-pipeline-model]]._

- [x] **MiniMax completion endpoint missing `/v1` segment** — fixed `_call_anthropic` to strip a trailing `/v1` from `base_url` before appending `/v1/messages`. Live-verified: the failover chain now reaches MiniMax's `/v1/messages` (200), distill batch of 3 returned clean domains without 404s. *(found & resolved 2026-07-06)*
- [x] **MiniMax extends-thinking blocks can swallow short responses** — `_call_anthropic` now picks the last `type:"text"` block in `content[]` (Anthropic-compat providers may chain thinking before text) and floors `max_tokens` to 4096 so thinking doesn't eat the entire answer budget. *(found & resolved 2026-07-06)*
- [ ] **Step 5 — Feature-identify per repo** — extend the distill output (currently purpose/entities/domain) into an explicit per-repo **feature** list, using the hub's ordered+typed (read/analyse/visualise) context as input to the LLM *(found 2026-06-30)*
- [ ] **Step 6 — Recommend feature-absorbs** — recommend which *features* from stars/forks to absorb into which owned repo; surface own-the-fork-and-delete actions and **unstar** recommendations for stars whose features are unwanted or already covered more fully by an owned repo. (Reframes the current repo-level `absorb` verdict toward feature-level.) *(found 2026-06-30)*
- [ ] **Step 7 — Align design principles** — once a hub's repos are owned + ordered, align their design principles (structure/docs/tests baseline) across the owned library *(found 2026-06-30)*
- [ ] **Step 8 — Guided installer (hub DAG)** — turn the plan into a guided installer where the hub is a DAG from git-suite to modular hub apps/info it reads; git-suite stays the install brain, does not build the hub apps *(found 2026-06-30)*
- [ ] **'Absorb' vocabulary carries the old meaning** — verdict `absorb`, `absorb_pct`, Triage "absorbs", README "Absorbs:" all mean repo→hub ingestion; the intended sense is feature→owned-repo. Decide rename vs. clarify when Step 6 lands (deferred, not urgent) *(found 2026-06-30)*

## Resolved

- [x] **Enrich stopped after one batch (3/3) + stale backend** — two causes. (1) The running backend on :2800 was a process from *before* the failover fix, so a single minimax 404 still killed the loop; restarted it on the latest code. (2) Enrich was a single all-or-nothing call that stopped on the first error. Reworked it into a **resumable batch loop**: `POST /scan/distill/{sid}?limit=N` distils the next N un-cached repos (`stop_on_error=False` — a transient repo failure is skipped, not fatal; only hard credit/quota sets `stop_reason`) and returns `cached/remaining/total`; the frontend loops batches of 25, filling the table incrementally, with a **Stop** button and resume-on-reclick. Added `distill.uncached()`. Verified live: a batch returns `done:5, remaining:833`. *(found & resolved 2026-06-30)*
- [x] **Enrich showed empty Purpose/Domain/Entities for owned repos** — the Records table looked distill records up by `r._full_name` (a field that doesn't exist; it's `full_name`), so owned rows fell back to the short name and never matched records, which distill keys by `full_name`. Stars (keyed by `full_name`) worked; owned never did. One-char fix; also restored the heads/README lookup for owned. Renamed user-facing "Distill" wording to "Enrich". Validated end-to-end: 3 owned repos distilled with real domains + cached. *(found & resolved 2026-06-30)*
- [x] **Enrich failed with "LLM: HTTP 404"; the failover chain didn't fail over** — `llm.complete` only advanced to the next provider when `_should_failover` matched a credit/quota/transient phrase, so a misconfigured leading provider (minimax returning 404 from a wrong model/endpoint) `raise`d immediately and killed the chain — ollama/openrouter were never tried. Now it fails over on **any** provider error (only raises when the last provider fails), and the final error lists every provider's failure. Verified live: the chain now skips minimax's 404 and a downstream provider completes. *(found & resolved 2026-06-30)*
- [x] **Pull never showed "complete" until tab-switch** — `pullForksAndStars` set `status='done'` only *after* awaiting the stars network call (which `gh_get` can now make slow under rate pressure), and *across* an `await`, so the page looked frozen. Restructured: the WS "done" flips `status='done'` synchronously (same callback that already renders the live repo count), and stars load in the background with a live "pulling stars…" indicator. *(found & resolved 2026-06-30)*
- [x] **Tests hit the dev's real LLM config (flaky + 108s)** — exposed by the failover fix: with no isolation, distill's LLM calls reached the real `~/.git-suite/config.json` (DNS-failing in CI), and the cluster tests' `hash()`-based embed fallback made grouping non-deterministic. Added an autouse `_isolate_llm` conftest fixture (empty config → deterministic instant fallback); suite back to ~4s. *(found & resolved 2026-06-30)*
- [x] **"Invalid token" was really a rate limit; `heads` ate the API budget** — the token was valid; `/user` + `/user/repos` were 403ing with "API rate limit exceeded", and `login`'s `validate_token` reported any failure as "Invalid GitHub token". Two fixes: (1) `validate_token` now raises `GitHubRateLimitError` for a throttling 403/429 and `login` returns **429 "rate-limited, token is fine"** instead of 401; only a real 401 / no-signal 403 is "invalid". (2) **Root cause — the `heads` burst** (one live GitHub GET per repo *and* per star) ran automatically on every pull/rehydrate, ~700 calls, exhausting the hourly budget. Removed `heads` from the automatic path (`refreshMeta` is now DB-only distill records); added an explicit **🔎 Check access** button for when you want the "Need attention" panel + README links. A pull is now ~10 API calls. *(found & resolved 2026-06-30)*
- [x] **403 on the first repos pull = GitHub *secondary* rate limit** — core quota was fine (4986/5000); the 403 came from the request *burst*: the scan pulled `/user/repos`, then `refreshForks` pulled it *again*, then `heads` fired one GET per repo **and** per star at 8-way concurrency. Ported homelab-designer's rate handling (`backend/scrapers/base.py`) into `services/github.py`: `gh_get` with `_rate_limit_wait` (disambiguates 429 / secondary-Retry-After / primary-remaining-0 / genuine-auth) + `_quota_sleep` (pre-emptive slow near quota) + `GitHubAuthError` for real 401/403. `list_repos`/`list_starred` route through it; `heads` is now rate-aware with concurrency 8→4. **Cut the burst**: the scan now snapshots the fork table from its own rows, so there's no second `/user/repos` pull (frontend drops `refreshForks` from the pull). +6 tests (`test_github.py`). 109 green *(found & resolved 2026-06-30)*
- [x] **Pipeline declutter — dropped Replan, Overlap, dead Fit backend; merged Hub Audit into Summary** — audit of pages vs the intended pipeline. (a) **Removed the Replan feature** (LLM auto-proposal loop — conflicts with the manual-promotion model): `routers/replan.py`, `services/replan.py`, `test_replan.py`, the `replan_pass`/`proposal`/`plan_history` tables, the `/replan` page + nav + api methods. (b) **Removed the Overlap page/feature**: `routers/overlap.py`, `test_overlap.py`, the `/overlap` page + nav + `getOverlap`/`setHubBoundary` api methods (the `/plan/hub-boundary` backend endpoint is kept for future boundary editing). (c) **Removed the orphaned Fit/revalidate backend** (dead after Fit left the Pull page): `scan.py` revalidate/verdicts endpoints + `_load_cluster_map`/`_tally`, `distill.revalidate` + `_SYS_RV`, the `repo_verdict` table, the distill tests. (d) **Merged Hub Audit into Summary** (hub members + orphan repos), deleted the `/layer-audit` route + the "Hubs" nav link. Nav is now Setup → Scan → Cluster → Own → Order → Triage → Execute → Summary. Docs synced (README + ui/ROADMAP nav, loop, concepts, backend layout). 103 green *(resolved 2026-06-30)*
- [x] **Clustering removed from the Pull path entirely** — two causes. (1) A leftover saved `cluster_result` row meant the Pull page's `saved_only` backfill *returned* clusters and populated the Cluster column — looked like it re-clustered even though no compute ran. (2) The Cluster page auto-computed on mount (`load()` → `recompute=false` with no saved → backend computed). Fixes: Pull page now has **zero** cluster code (no `getClusters`, no Cluster/Fit columns, no revalidate; Enrich = distill only; Hub stays a plan-only backfill). Cluster page mount uses `saved_only` so it **never** auto-computes — clustering runs only on an explicit Re-cluster / slider / source / refresh, with a "🧩 Cluster now" empty state. Removed dead `revalidate`/`verdicts` api methods. Wiped `cluster_result` + `repo_verdict` from the live DB. *(found & resolved 2026-06-30)*
- [x] **Scan = one staged GitHub pull; stars failures surfaced** — reframed the page as a single GitHub pull with three visible phases (your repos → forks → stars), status text naming each as it happens. Root cause of "stars don't work": the old `afterScan` ran `Promise.all([refreshForks, refreshStars])` under one catch whose `errorMsg` the done view never rendered, so a forks failure (or rate-limit) aborted the batch before stars and showed nothing. Now each phase is independent with its own surfaced error (`pullErrors`). Dropped clustering language from the scan action/status (heading is "GitHub Pull"; Cluster/Hub stay read-only backfill columns). *(found & resolved 2026-06-30)*
- [x] **Scan still triggered clustering on first run** — the Scan page's "backfill" call (`getClusters` with `recompute=false`) only short-circuits when a saved result exists; on a first scan there's none, so the backend *fell through and computed* a full clustering pass (embedding token spend). Added a `saved_only` mode to `GET /cluster/{sid}` that returns the saved result or `{available:false}` and never computes; Scan's backfill now passes it. Only the explicit Cluster action (`recompute=true`) ever clusters. *(found & resolved 2026-06-30)*
- [x] **Scan page muddled (5 buttons)** — collapsed to two clear actions: **A) Scan / Re-scan** (repos + forks + stars) and **B) Enrich** (LLM pass filling Fit + Purpose + Domain + Entities = distill + revalidate in one). Removed the standalone Distill / Cluster / Revalidate buttons — clustering belongs to the Cluster step. The **Cluster** and **Hub** table columns are now backfilled read-only from the Cluster step's saved result + the live plan (reconcile), not computed on Scan. *(resolved 2026-06-30)*
- [x] **Landing resumes the last step** — root `/` used to always redirect to `/setup`. Now it resumes the last workflow route visited when logged in, else falls back to `/setup` (the first step). New persisted `lastStep` store written by the layout on every nav. *(resolved 2026-06-30)*
- [x] **/cluster 500 on direct load (SSR `window`)** — `cluster/+page.svelte`'s `onDestroy` called `window.removeEventListener` unconditionally; Svelte 5 runs `onDestroy` during SSR, so a fresh load/refresh of `/cluster` threw `ReferenceError: window is not defined` (client-side nav masked it). Guarded the listener cleanup with `browser`. *(found & resolved 2026-06-30)*
- [x] **Step 3 — Own (promote fork → owned repo)** — new "Own" stage (nav: Cluster → **Own** → Order). Since GitHub has no API to detach a fork, promotion is a *decision + git checklist* (same model as migration assist), not a fake API call. Backend: `services/promote.py` (LLM/rule detach checklist) + `routers/promote.py` — `GET /promote/{sid}` lists owned forks (scan `is_fork`) enriched with upstream status via `_head_one` (parent, private-upstream flag), current plan verdict, and cluster assignment; `POST /promote/decide` maps promote→absorb-into-hub / promote-no-hub→keep / drop→archive onto plain `plan_store.set_verdict` (one source of truth); `POST /promote/checklist/{sid}` generates a per-fork detach checklist (mirror-push or leave-fork-network → re-point remotes → cut upstream sync → optional hub-standardise → archive/delete old fork). Frontend `/promote` page. +5 tests (110 green) *(resolved 2026-06-30)*
- [x] **ponytail-audit cleanup (round 7) — dead Hub-detail/Stars backends** — repo-wide audit, applied the 5 clean dead-code cuts left behind when the Hub-detail and Stars pages were folded into Cluster/Execute. Deleted the **commercial-refs feature** end-to-end (`routers/commercial.py`, `services/scraper.py`, `services/claude_ai.py`, the `commercial_refs` table, `_refs_for` + the scraped-benchmarks block in `readme.py`, `compose_section`'s `refs` param, api.js `scrapeUrl`/`deleteRef`, the Setup "Commercial — scrape" line) — which also dropped the **`beautifulsoup4`** dep + the dead crawl4ai comment (scraper was its only user). Deleted the **stars dedup engine** (`services/stars.py`, `GET /stars/dedup`, api `getStarsDedup`, `test_stars.py`) — snapshot path (`GET /stars`) kept. Removed the orphaned **`POST /plan/hub-alternative`** (+ `plan_store.add/remove_hub_alternative`), the dead **`GET /hubs/{hub}/status`** + **`POST /hubs/archive`**, and the dead **`POST /readme/push`** + **`GET /readme/preview`** route handlers (Execute pushes via `/execute/push-readmes`; `readme.py` is now a pure service module, dropped from `main`'s includes). Tests trimmed accordingly. ~500 lines + 1 dep gone. 105 green *(resolved 2026-06-30)*

- [x] **Hardcoded hub taxonomy in replan/overlap keyword rules** — same seed problem one level deeper: `services/replan.py` carried `HUB_KEYWORDS` + `LANG_HINTS` mapping repos to the 8 curated hub *names*, and `routers/overlap.py` reused `_score_hub` for its keyword fallback — both assume hubs that, post-seed-removal, won't exist for a fresh user (and ignore the ones that do). Since clustering itself requires embeddings, any portfolio that *has* hubs already has embeddings, so the keyword tier was effectively dead and only useful when bound to a hardcoded taxonomy. Deleted `HUB_KEYWORDS`/`LANG_HINTS`/`_score_hub`/`_rule_proposal`/`_RULE_THRESHOLD`; replan orphan determination is now embeddings (against the actual plan hubs) → LLM → default-keep. Overlap is semantic-only and degrades to `method:"unavailable"` (like cluster) instead of a keyword fallback; frontend shows "embeddings required". Tests reworked (embedding path stubbed offline; semantic overlap mocked). 117 green *(resolved 2026-06-28)*
- [x] **Remove the seed entirely — no hubs that don't come from GitHub** — making the production seed blank still left a curated taxonomy wired in: `plan.py` (8 named hubs + absorbs + 37 archives + keeps), `plan_store._curated_plan()` built a plan from it, and `_heal()` silently backfilled curated description/boundary/alternatives onto any hub that shared a curated name — hidden seeding. Deleted `plan.py` and `_curated_plan()`; `_heal()` is now structural-only (ensures keys/defaults, drops legacy `layer`, never invents content). The taxonomy survives only as **test-fixture data** in `conftest.py` (`_sample_plan()`), clearly not wired into the app, so the suite still has hubs to exercise. Reverted the mistaken "Load reference plan" button/endpoint (it loaded exactly this seed). Updated `ui/ROADMAP.md`. 119 green *(resolved 2026-06-28)*
- [x] **Hub column backfilled from the seed before the hub stage** — the Scan records table showed a Hub per repo even before any hub was formed, because `plan.py` hardcoded `HUB_ABSORBS` (every hub's members) + `ARCHIVE_HUB` (repo→hub) and `repo_placement()` mapped those on every scan. Same hardcoded-taxonomy problem as layers, one level down. Per decision, the production seed is now **fully blank** (`_seed_plan() → {"hubs":{},"archives":{},"keeps":[]}`) — nothing is assumed to be a hub; hubs emerge from clustering→promote/create. The curated plan.py dicts are kept as a dormant reference (`_curated_plan()`), not auto-seeded; `_heal()` still backfills metadata from it onto any hub that carries a curated name, and `blank()` now clears assignments off the *current* plan's hubs (not the seed). Test fixture `isolated_plan` loads `_curated_plan()` so existing hub-dependent tests are unchanged; added `test_production_seed_is_blank`. Runtime `plan.json` (both `~/.git-suite` dev and `data/backend` docker volume) blanked, each backed up to `.bak`. 119 green *(resolved 2026-06-24)*
- [x] **Clustering is a separate, on-demand step (don't blitz tokens on scan)** — Scan no longer auto-fires clustering. `enrich()` (post-scan) now only snapshots forks/stars and loads any *saved* cluster result (no recompute, no token spend); added a `🧩 Cluster repos` button next to `✨ Distill all` that calls `getClusters(recompute:true)` when you choose. Distill + Cluster are both resumable steps you trigger explicitly. Page subtitle/status text updated. *(resolved 2026-06-24)*
- [x] **Hubs must group by SUBSTANCE, not tech stack** — reworked the distill prompt: domain is now "the activity, hobby, or line of work the repo serves" (e.g. tabletop role-playing, home server admin, IT support), explicitly forbidding tech-stack/computing-category answers ("web development", "automation", "ML", "API"). Bumped `_PROMPT_VERSION` and folded it into the distill cache key (`_cache_hash`) so existing tech-flavoured records re-distill on the next run. Cluster *names* now derive from distilled domain+entities (substance), not raw tech topics. *(resolved 2026-06-24)*
- [x] **replan `IndexError` on single-hub portfolios** — `generate_proposals` did `er[1][1]` (the "next best hub" margin) unconditionally; with one hub `emb_rank` has length 1 → crash whenever the top cosine cleared 0.28. Guarded: margin falls back to 0.0 with a single hub, rationale omits the "next" note. *(found & resolved 2026-06-24)*
- [x] **Strip hardcoded hub ordering (layer→emergent) from EVERY level** — gutted the seed: `plan.py` HUB_META no longer carries `layer`/`priority`, and the fixed `LAYER_NAMES` 0-9 taxonomy is deleted. Hub ordering is now emergent — `plan_store.hub_sort_key(priority, size, name)` orders by optional manual `priority` first (unset last), then hub size (absorb count, largest first), then name. `priority` is the lone optional override, set at promote/create (default `None`); `layer` is removed everywhere. `plan_store` (`_seed_plan`/`_heal`/`blank`/`clear`/`upsert_hub`) drops layer + layer_names and self-heals legacy plan.json (pops `layer`/`layer_names`). reconcile drops the layers_roll block, attaches `repos` per hub, sorts emergently, no longer returns `layers`. readme/migration drop the `Layer X —` prefix; replan drops the `(L#)` prompt tag **and the empty-layer→new-hub advisory** (the clearest fixed-taxonomy artefact). Frontend: layer-audit page → "Hub Audit" (renders `data.hubs` per-hub, dropped the duplicated LAYER_NAMES const), nav label Layers→Hubs, triage/summary drop the layer tag/sort. Tests updated across cluster/order/plan_store/walkthrough/replan/migration. 118 green *(resolved 2026-06-23)*

- [x] **ponytail-audit cleanup (round 6)** — repo-wide audit, applied the safe cuts: deleted the dead `GET /scan/{scan_id}/results` endpoint (no callers, superseded by `latestScan`); removed a duplicate `import distill_svc` inside `revalidate()`; hoisted mid-file imports (`asyncio`, `httpx`, `github` helpers, `hashlib`) to the top of `scan.py`; hoisted `asyncio` + the `_SYS_RV` revalidate prompt to module level in `distill.py`; `embeddings.DEFAULT_PRIORITY` now derives from `EMBED_PROVIDERS` keys (one source of truth); refreshed the stale cluster.py docstring (union-find → spherical k-means); deleted the no-op `warnings_sorted` reactive on the Scan page (always returned 0). **Partial progress on the layer issue below:** `FormRequest.layer/priority` default to `None` (was hardcoded 9/3) and `plan_store.upsert_hub` stores `None` as null instead of coercing — a new hub no longer inherits a hardcoded layer. Skipped the records/revalidate gather-loop dedup (a higher-order fn over `nonlocal` closures is the clever-at-3am thing the audit guards against). 118 tests green *(resolved 2026-06-23)*
- [x] **Revalidate button + drift badges** — added a 🔁 Revalidate clusters button to the Scan page (next to Distill, disabled until clustering has run). Calls `POST /api/scan/distill/revalidate/{sid}`, which re-asks the LLM "is the repo's purpose still consistent with the cluster it landed in?" per repo and returns `fit` / `drift` / `mis-clustered`. Verdicts are cached in `repo_verdict` keyed by `cluster_hash` (sha256 of the saved cluster_result); a saved verdict stays valid until the clustering or the repo's purpose changes. New `GET /api/scan/distill/verdicts/{sid}` returns the cached verdicts for the *current* cluster hash so the badges render on every page load (no LLM round-trip). UI: row badges (fit=green / drift=amber / mis=red, with row tint), drift banner with counts + cluster snapshot hash. 120 tests green *(resolved 2026-06-23)* — distillation moved onto the Scan page as a separate `✨ Distill all` step (loops the same strict-JSON prompt over every repo). Inputs to the LLM: name, description, topics, repo URL, **README URL** (built from `default_branch` — we don't fetch contents, just hand the URL to the model). Output: `{purpose, entities[], domain}`. Cached in `repo_domain` by `src_hash`; regen only on change. The loop stops on the first credit-exhausted / quota / 402 / 429 error and reports the count + reason; resumes next run from where it stopped (cached rows skip). Cluster now embeds `domain + entities` (not purpose) — purpose is what humans re-read on hover. Revalidate pass (`/api/scan/distill/revalidate`) re-asks the LLM "is the purpose still consistent with the cluster it landed in?" and returns fit/drift/mis-clustered. Heads endpoint (`/api/scan/heads/{sid}`) does one cheap `GET /repos/{owner}/{repo}` per owned+starred, surfaces private-upstream forks + 403s + 404s in a "Need attention" panel with one-click links so archived or 403'd stars can be unstared. `repos.full_name` is now persisted (schema migration + new `repos.full_name` column) so every downstream key (heads, distill, cluster) lines up. 120 tests green *(resolved 2026-06-23)* — two root causes. (1) Algorithm: single-linkage union-find chained A–B–C transitively. First replaced with greedy leader clustering, but that still produced poor splits — superseded by **spherical k-means** (`_kmeans`, pure-Python Lloyd's + deterministic farthest-point init, target K, empty clusters dropped). UI swapped the tightness slider for a **# clusters** slider (default ~√(n/2)). (2) Signal: nomic was embedding raw blurbs dominated by generic "it's software" framing, so everything sat in a tight 0.55–0.7 band. Added `services/distill.py` — an LLM pass (cached in `repo_domain` by source-hash, regen-on-change, graceful raw-text fallback) that compresses each repo to a one-line **semantic domain** (entities/purpose, no "software"/language). That distillation, prefixed with nomic's `clustering:` task token, is what gets embedded. Domain shown on the cluster hover card. Tests: `test_kmeans_*` (partition/no-chain/clamp) + `test_distill.py` (cache hit, regen, fallback). 115 green *(resolved 2026-06-23)*
- [x] **Cluster recomputed every tab load + wasn't saved** — added `cluster_result` table (one JSON row per session). `GET /cluster/{sid}` now returns the saved payload (flagged `saved:true`) without re-embedding; `?recompute=true` forces a fresh pass and overwrites. Cluster page loads saved on mount (syncs the tightness slider to the saved threshold), and only recomputes on Re-cluster / tightness / source / refresh. *(resolved 2026-06-23)*
- [x] **Scan didn't include forks/stars; no record table** — Scan is now one pass: stream owned repos → snapshot forks+stars (reuses refresh-forks/refresh-stars) → recompute+save clustering. Page renders a unified records **table** (source glyph 📦/🍴/⭐, repo, cluster label, category, hub, lang, ★) with the cluster assignment per row from the saved result. Own forks come from the scan rows (`is_fork`); stars from `GET /stars`. Cluster column falls back to "—" with a Setup hint if embeddings are down. *(resolved 2026-06-23)*
- [x] **Cluster D3 viz + refresh forks/stars** — rewrote `/cluster` as a `d3-force` bubble layout (one node per cluster, sized by member count); hover opens+pins a panel with editable name/description, per-member Promote (preferred) and a central Create button. Added `↻ Refresh forks/stars` (reuses refresh-forks + refresh-stars endpoints) so the mixed source has data. Added `d3-force@^3` dep (rendered via Svelte, not d3-selection). Layer no longer set at cluster time (emergent). *(resolved 2026-06-22)*

- [x] **Cluster empty in Docker (embeddings ollama → localhost)** — `services/embeddings.py` hardcoded the ollama `base_url` to `http://localhost:11434`, which inside the container resolves to the container, not the host (connection refused → `build_clusters_mixed` returns None → page shows "Embeddings not configured"). `llm_providers.py` already defaulted to `host.docker.internal:11434` + `LLM_OLLAMA_BASE_URL` override; embeddings now reuses that resolved URL as the single source of truth. Scan/reconcile were never broken (88 owned orphans present). *(resolved 2026-06-22)*
- [x] **Drop Hubs + Stars pages; fold into Cluster/Execute** — removed `/hubs`, `/hubs/[hub]`, `/stars` routes and their nav links (hubs are now formed from Cluster via promote/new/existing; stars feed the mixed cluster space). Repointed stray `/hubs` links (scan, order, summary) to `/cluster`. Folded a slim "Finish absorbs" section into `/execute` (per-hub mark-absorbed + push MIGRATION.md, reusing the intact migration API); skipped the old detail page's LLM checklist gen / URL scrape / README preview. Backend hubs/stars routers kept (cluster/reconcile/migration still use them). *(resolved 2026-06-22)*
- [x] **Phase 4: tests + walkthrough** — added router tests for the two modules that only had service tests: `tests/test_migration_router.py` (5: status mapping + live/done flags, 404 unknown hub, 404 non-absorb repo, checklist cache hit on second call) and `tests/test_stars_router.py` (3: empty snapshot returns count 0, dedup unavailable when no stars, dedup 404 when no scan). Replaced the planned "browser walkthrough" with `tests/test_walkthrough.py` — one TestClient test that (a) verifies every nav route is wired via `app.url_path_for`, and (b) asserts the JSON shape the offline routes return. Network-dependent routes (reconcile/replan/overlap/execute) are tested for wiring only — their bodies are covered by their own focused tests. 111 green *(resolved 2026-06-21)*
- [x] **ponytail-audit cleanup (-~1456 lines, -1 dep)** — deleted the legacy CLI trio superseded by the app: `generate_github_index.py` (1072-line CSV/xlsx pipeline + sole `openpyxl` importer), `portfolio_review.py` (review phases duplicated Reconcile/Summary/Layer-audit; read the dead CSV), and the stale `github_index.csv`. Collapsed `services/scraper.py` to the httpx+BeautifulSoup path (crawl4ai branch never fired — no 3.14 wheel, optional dep). Cleaned README/plan.py/.gitignore refs. 102 tests green *(resolved 2026-06-21)*
- [x] **Absorb targets include non-owned repos** — the old "100% absorbed from day one" symptom was already gone (reconcile's `absorb_pct` counts executed actions, not membership). The live risk was that `prune-ghosts` / the `ghost-prune` proposal silently deleted external "absorb the functionality of" targets (e.g. `autoEdit_2`, `intelli-video`) — ghosts by design, never in any scan. Fix without a schema split: reconcile now marks each ghost `was_live` (ever appeared in ANY of the session's scans) and reports `ghost_deletable` vs `ghost_external`; prune + the proposal only touch `was_live` ghosts. Replan UI prunes/labels the deletable count and shows external separately. True external inspiration belongs in `alternatives` (issue #1) now. +2 tests *(resolved 2026-06-21)*
- [x] **fetch_following() not paginated** — non-issue: the following list is no longer fetched anywhere (simplified script). Closed; if re-added, paginate beyond 100 *(closed 2026-06-21)*
- [x] **Stars feed HUB_ALTERNATIVES** — Stars page per-hub suggestions got a `+ alternative` button that accepts a starred project into the hub's `alternatives.oss` via new `POST /plan/hub-alternative` (+ `plan_store.add/remove_hub_alternative`, idempotent); hub-detail alt chips got a `×` to remove. Alternatives are no longer seed-only. +1 test (`test_add_and_remove_hub_alternative`) *(resolved 2026-06-21)*
- [x] **L9 Creative hub** — added `creative-hub` (L9) to the seed; `VTuberLIVE` moved from `KEEP_AS_IS` into its absorbs so it has a layer home. Execute will create the repo (currently `[--]` on GitHub). Frontend L9 label added to hub-detail map. +1 test (`test_creative_hub_homes_vtuberlive`) *(resolved 2026-06-21)*
- [x] **In-place edit of an existing hub's meta** — Hubs page `✎` button pre-fills the existing add form (name locked) so layer/priority/description/boundary edit in place via the `/plan/hub` upsert, which already preserved absorbs; `/hubs` now returns `boundary` so it round-trips without being blanked. +1 test (`test_upsert_edits_existing_hub_keeping_absorbs`, 99). `alternatives` editing left to issue #1 (stars→alternatives) *(resolved 2026-06-20)*
- [x] **Cross-source cluster + per-hub Order page (Tree-of-Knowledge layout)** — built the Order feature across 5 stages: (1) `services/columns.py` (single source of truth for the three ToK column names: Gather / Analyse / Display — renaming is a two-file edit) + `database.py` `fork`, `hub_order`, `hub_compat_tags` tables + `services/cluster.build_clusters_mixed(owned, forks, stars, threshold)` that embeds all three sources in one space and tags each member with its source. `?source=owned` keeps the legacy owned-only path. (2) `routers/order.py` (GET/POST order, suggest-order, suggest-column, compat-tags, annotate) — all LLM calls through the existing `services/llm.complete()` failover chain. (3) frontend `src/lib/columns.js` mirror, cluster page `[O]/[F]/[S]` prefix symbols + per-source counts + source-mode toggle, Hubs `→ Order` link, Order inserted in nav between Hubs and Stars. (4) `routes/order/+page.svelte` — hub picker, one ordered list per hub, per-row up/down/top/bottom arrows + three classification checkboxes + per-row `✨ Suggest` button + top-of-page `✨ Suggest order` (LLM proposes a ToK-aware reorder with per-move rationales; user accepts all or discards) + per-hub compat-tag chips + single-step Undo + Save (reassigns 0..N-1 positions before POSTing). Drag-and-drop deliberately deferred — arrows cover the realistic ~30-node hub size without adding a dependency; `svelte-dnd-action` is the upgrade path. (5) `routers/readme.py compose_section` reads `hub_order` and renders a new `### Tree-of-Knowledge ordering` subsection in the hub README (hub repo pinned to top, column flags as a tag line, compat tags as a sub-bullet, feature annotations as individual sub-bullets; absorbs without a row fall back to alphabetical at the tail with a `_(not yet ordered)_` marker so no member is silently dropped). +22 tests (99). 5 commits: 17bd4af, e58f00f, ea1ba57, 577a372, 43129e9 *(resolved 2026-06-18)*
- [x] **State persistence: state.db + config.json + plan.json on a host volume** — `state.db` lived at `/app/state.db` (unmounted), `config.json` and `plan.json` at `/root/.git-suite/...` (also unmounted); the existing `./data/backend:/app/data` host mount was sitting there empty, unused — so every `docker compose up -d --build` silently wiped the SQLite sessions, the LLM keys, and the plan edits. Introduced `GIT_SUITE_HOME` env var; all three files now resolve under it. `docker-compose.yml` sets `GIT_SUITE_HOME=/app/data` for the backend, so the existing host mount covers everything. Default for non-Docker runs stays `~/.git-suite`. Verified: wrote config → rebuilt stack → file still on host and still served by the API *(resolved 2026-06-13)*
- [x] **Setup fixups: ollama host bridge, dead `gh auth` button, nav order, stale session** — (a) Ollama's `base_url` was `http://localhost:11434` which inside the Docker container pointed back at the container itself; changed to `http://host.docker.internal:11434` (Atelier-Harness pattern; `LLM_OLLAMA_BASE_URL` env var still overrides for non-Docker runs). (b) The `gh auth` button on Setup opened a flow whose handler had been removed; deleted the button and its source-label/prefill code. (c) Nav was Scan→Stars→Cluster→Hubs→… but the workflow is Scan→Hubs→Stars (you scan, decide what your hubs are, then check stars against that map); reordered to Setup→Scan→Hubs→Stars→Cluster→Overlap→Replan→Triage→Execute→Layers→Summary. (d) After a Docker rebuild the SQLite session row is gone but `localStorage.session_id` still points at it, so the first API call 401s; `api.js` now auto-clears the local session on a 401 with `/invalid session/i` so the UI drops back to the login card instead of being stuck *(resolved 2026-06-13)*
- [x] **minimax now uses the Anthropic dialect (align to Atelier-Harness)** — minimax had its own `api_type="minimax"` with `chatcompletion_v2` shape and no model-listing endpoint; `llm_providers.py` now declares it `api_type="anthropic"` with `base_url="https://api.minimax.io/anthropic"`, so both completions and the live model listing go through the existing Anthropic branch (x-api-key + Bearer + anthropic-version). `_call_minimax` and the minimax branch in `_dispatch` deleted. Setup page dropdown now lists the real minimax models; user can still type a name manually if the listing fails. Test updated: `test_minimax_uses_anthropic_dialect`. +8 tests (77) *(resolved 2026-06-13)*
- [x] **LLM setup: hardcoded call URLs + live model listing (no static lists)** — Setup used to expose a per-provider Call URL override (over-engineered: the URL is a standard element of each API, not user config) and a free-text Model field (static defaults rot; provider's API is the source of truth). Removed `llm_base_urls` from the config schema (legacy values scrubbed on save); `_dispatch` now reads `PROVIDERS[name]["base_url"]` straight from the registry. New `services/models.py` lists models live per dialect — `anthropic` dual-auth (x-api-key + Bearer + anthropic-version), `openai_compat` Bearer at `{base}/models`, `ollama` keyless at `{base}/api/tags`, `minimax` raises (manual entry) — exposed via `POST /api/config/models/{provider}` (POST so an unsaved key never lands in URLs/access logs). Result filtered to completion-capable models for `kind=llm` (drops whisper/tts/dall-e/realtime/moderation/embedding), to embed-* for `kind=embedding`. Setup page now has a live dropdown per provider with a `↻` refresh button + manual text input fallback when listing fails/returns empty. +8 tests (77) *(resolved 2026-06-13)*
- [x] **Stars as first-class dedup input** — built the Stars stage (nav: Scan→Stars→Cluster). Backend: `starred_repo` snapshot table, `github.list_starred` (paginated, own repos excluded), `services/stars.py` dedup engine (numpy cosine matrix over the embedding chain w/ DB cache; deterministic token-overlap fallback), `routers/stars.py` (refresh/list/dedup). Frontend: `/stars` page — snapshot refresh, duplication check with "Archive mine" verdict action per flagged repo, per-hub starred OSS-alternative suggestions, semantic/keyword method badge. +8 tests (69) *(resolved 2026-06-12)*
- [x] **repos_root + folder-picker machinery removed (remote-only)** — repos_root never drove anything (stored "for future clone/migration" only) and contradicted the remote-first principle; user confirmed it's not wanted. Removed: Setup form field, `/auth/pick-folder|browse|search-folder|path-complete|defaults` endpoints, api.js methods; login is token-only (DB column kept for schema compat, written as ""). This also retires the "Browse folder picker errors" open issue — the picker no longer exists *(resolved 2026-06-12)*
- [x] **Plaintext GitHub PAT in ui/backend/logs/app.log** — old aiosqlite DEBUG lines logged full session INSERTs including a `ghp_…` token (logging has since been quieted to INFO). File was gitignored, never committed; deleted from disk. ⚠ Revoke that classic PAT at github.com/settings/tokens if still active *(found & resolved 2026-06-12)*
- [x] **Login/Setup split — Setup not actually the first step** — GitHub login lived on a separate gate page at `/` while Setup only held LLM/embed config (docs claimed otherwise — drift). Merged: Setup now opens with a GitHub-connection card (token + gh-auth fetch + optional repos_root + connected-user/disconnect state); `/` redirects to `/setup`; `repos_root` made optional in both the form and the backend `LoginRequest` (401 not 422 without it — verified live); dead `FolderBrowser.svelte` removed. 61 tests green, stack rebuilt *(found & resolved 2026-06-12)*
- [x] **Setup movePriority crash** — `movePriority` referenced undefined `configured`; first reorder before a saved priority order threw ReferenceError. Now starts from the rendered `orderedConfigured` *(found & resolved 2026-06-12)*

- [x] **Stack down: 502 Bad Gateway on Save in Setup** — the curl-less healthcheck fix was already committed; rebuilt the stack with `docker compose up -d --build` (first attempt hit a transient Docker Desktop DNS failure to auth.docker.io; retry succeeded). Backend now reports `(healthy)`, nginx serves, `/api/plan` returns 200 through :8080. *(resolved 2026-06-12)*
- [x] **Frontend container served the wrong build dir — every page 404** — `Dockerfile.frontend` copied `.svelte-kit/output` (SvelteKit's intermediate build) instead of the adapter-static output `build/`; `serve -s output` returned a directory listing at `/` and 404 for every route, so the app was never actually served from Docker. Fixed to `COPY --from=0 /app/build ./build` + `serve -s build`. *(found & resolved 2026-06-12)*
- [x] **Functional flow reorder applied; verify pages** — all nav pages live-verified through nginx after the rebuild: /, /scan, /hubs, /triage, /summary, /setup all 200 with real app HTML. *(resolved 2026-06-12)*

- [x] **Root README was stale (old repo-per-layer model)** — rewritten off current state: describes git-suite as the consolidation *tool* (Setup→Scan→Cluster→Hubs→Overlap→Replan→Triage→Execute→Layers→Summary), the plan-as-data / remote-first / no-assumed-hub principles, the SvelteKit→nginx→FastAPI→SQLite architecture + failover chains, and Docker/dev/test run steps. Removed the dead belzona-tickets listing; points layer/hub detail at PLAN.md *(resolved 2026-06-06)*
- [x] **Setup: Add-provider opened no form + per-provider call URL** — "Add provider" set an empty key which filtered the form straight back out; now tracks added providers by registration. Each provider configures API Key + Call URL (base_url override) + Model with a "get key" link, sourced from a provider registry (`/api/config/providers` returns display_name/base_url/setup_url/default_model/needs_key). `llm_base_urls` persists and `llm._dispatch` honours the override *(resolved 2026-06-03)*
- [x] **Setup: dead Jira/Zoho config (belzona carryover)** — removed jira/email/api_token/zoho_* from the config schema and Setup form (git-suite has no ticketing); replaced with a "Where these are used" card mapping each failover chain to its call sites — LLM: Replan/Migration/Commercial; Embeddings: Cluster/Overlap/Replan *(resolved 2026-06-03)*
- [x] **Assisted clustering stage ("Form groups")** — Cluster page (nav between Scan/Hubs): embeds unassigned repos, union-find clusters over a cosine threshold (tightness slider), suggests a theme; user names a new hub / promotes a member / adds to existing, with a description that feeds the hub's LLM boundary; forms hub + absorbs members. Backend services/cluster.py + routers/cluster.py; degrades to available:false without reachable embeddings. +6 tests (61) *(resolved 2026-06-03)*
- [x] **Functional flow reorder** — nav: Setup→Scan→Cluster→Hubs→Overlap→Replan→Triage→Execute→Layers→Summary; Archive folded into Execute *(resolved 2026-06-03)*
- [x] **Portable scan WebSocket (A)** — WS URL derives from window.location (ws/wss), proxied by Vite (ws:true) + nginx (upgrade headers); works dev/docker/https *(resolved 2026-06-03)*
- [x] **Embeddings semantic layer (B)** — services/embeddings.py (OpenAI + opt-in Ollama, failover, DB-cached, cosine); /api/overlap does semantic cosine analysis with keyword fallback (reports method); replan proposes absorb from a clear embedding match (source 'embedding'); embedding cache table. Degrades to keyword/rules with no provider. +6 tests (54) *(resolved 2026-06-03)*
- [x] **Overnight improvements (2026-06-03)** — (1) plan_store self-heals new hub fields (boundary etc.) on load, no reset needed; (2) logging quieted to INFO, aiosqlite/httpx DEBUG silenced; (3) **hub stub lifecycle**: github unarchive_repo/delete_repo + Execute archive-hubs/return/delete (delete gated to already-archived) + UI; (4) ROADMAP rewritten to actual architecture; (5) Summary surfaces stub count + review action. 49 tests green. On branch feature/overnight-improvements *(resolved 2026-06-03)*
- [x] **Phase 6: hub boundaries + semantic overlap (venn)** — per-hub editable `boundary` rule (seeded from delegation notes) fed to replan + migration LLM prompts; `/api/overlap` scores repos vs hub keyword/topic profiles → boundary cases + hub×hub matrix; Overlap page (matrix heatmap + cases list + boundary editor). Rule+topic method, seed+editable boundaries, matrix+cases viz (per decisions 2026-06-03). +2 tests (44) *(resolved 2026-06-03)*
- [x] **Phase 5 (partial): fresh-start UI** — Replan page got "Prune N ghosts" + "Start fresh (blank plan)" buttons (with confirm); Triage shows stub badges. Backend blank/prune/stub already landed *(resolved 2026-06-03)*
- [x] **Phase 3: Migration assist** — `services/migration.py` (checklist_for via LLM failover + per-language rule template; scaffold_for; build_migration_md) + `github.get_readme` (on-demand) + `routers/migration.py` (GET hub scaffold/status, POST generate+cache checklist, POST push MIGRATION.md) + migration_checklist DB table. Hub detail gained a Migration section: per-absorb generate/regenerate/view-steps + Push MIGRATION.md. Live-verified game-hub (21 absorbs, 7-step rule checklist cached). +5 tests (37 total) *(resolved 2026-06-02)*
- [x] **list_repos missed private repos** — used /users/{username}/repos (public only), so scan/reconcile/execute saw 118 of ~144 repos and flagged private hubs (personal-ai-os, ontology-align) as "missing". Switched to /user/repos?affiliation=owner&visibility=all. Re-scan in UI to capture the full portfolio *(resolved 2026-06-02)*
- [x] **Phase 2: Execute only archived; READMEs used stale seed** — (a) readme.py routed through plan_store with reusable compose_section/push_hub_readme/readme_status; (b) github.create_repo + get_file added; (c) Execute expanded to 3 dry-run groups — archive, create-missing-hubs, push-READMEs (diffed vs composed) — each idempotent with select+run UI; (d) hub detail shows plan OSS/commercial alternatives. Live-verified: 11 archive / 13 already / 0 create / 8 README stale. +9 tests (32 total) *(resolved 2026-06-02)*
- [x] **Phase 1a: scan was signal-poor** — enriched scan with topics, stars, fork, pushed_at, archived (DB migration + scan capture + reconcile passthrough); replan rules now also score against topics *(resolved 2026-06-02)*
- [x] **Phase 1b: plan data duplicated/drifting** — plan.py made canonical (37 archive targets, was 20); portfolio_review.py now derives its plan from the seed (duplicated dicts removed) so the two can't drift; stale .xlsx deleted (.venv already git-ignored). Reconcile orphans dropped 19→0 *(resolved 2026-06-02)*
- [x] **8 hub repos showed as orphans** — repo_placement now treats any hub repo as implicitly 'keep' (a hub is the destination, not a candidate); fix applies across scan/reconcile/execute/replan *(resolved 2026-06-02)*
- [x] **Archive count drift (PLAN.md 35 vs plan.py 20)** — plan.py ARCHIVE_HUB restored to the full 37 (incl. no-hub retire-anytime targets); plan_store reseeded *(resolved 2026-06-02)*
- [x] **Phase 1c: no automated tests** — added pytest suite (26 tests) covering plan_store verdicts/placement, reconcile diffing, replan two-phase generation + rules, execute idempotency, llm failover; + pytest.ini + requirements-dev.txt. All green *(resolved 2026-06-02)*
- [x] **LLM had no failover — single provider, no resilience** — added `llm_providers.py` (registry: api_type/base_url/default_model/exhaust_patterns, adapted from belzona-tickets) + `services/llm.py` (async failover client: builds chain from config keys+models+priority_order, advances past exhausted/transient providers, persists floor, keyless Ollama opt-in only). Routed `claude_ai.extract_features` + `replan._llm_proposal` through it. Added `GET /api/config/llm-status` + Setup page chain readout. Tested: anthropic→openai failover on credit error, subsequent calls skip dead provider, empty-config degrades to rules-only *(resolved 2026-06-02)*
- [x] **No execute step — planning never reached GitHub (philosophy #4)** — added `routers/execute.py`: `GET /api/execute/preview` (dry-run: archive-verdict repos checked against *live* GitHub state → will/already/gone) + `POST /api/execute/archive` (batch, idempotent — skips already-archived/gone, records hub_actions). Frontend `/execute` page: grouped preview, per-repo select, explicit confirm checkbox, batch run, results. Live-tested: 11 will-archive (matches reconcile), skip path verified, no real archive fired *(resolved 2026-06-02)*
- [x] **Stack never booted live (only TestClient/build)** — verified under real uvicorn: lifespan creates new tables, /health + /api/plan + /api/reconcile + /api/replan/state + /api/execute/preview all serve 200 *(resolved 2026-06-02)*
- [x] **Scoping was a one-shot LLM call, not a loop** — added an iterative re-planning loop: `services/replan.py` engine (hybrid keyword/language rules + LLM fallback, degrades to rules-only with no API key) + `routers/replan.py` (`/api/replan/pass`, `/state`, `/proposals`, accept/reject, `/history`) + DB tables (replan_pass, proposal, plan_history) + `/replan` UI page. Two-phase state machine: incremental (fill orphans/prune ghosts) until fully planned, then structural replan (split/new-hub advisories). Validated: 45 proposals from live scan, accept applies via plan_store + logs history *(resolved 2026-06-02)*
- [x] **8 hub repos showed as orphans** — replan engine now self-keeps any repo that is itself a hub (proposes verdict=keep, conf 0.95) *(resolved 2026-06-02)*
- [x] **Plan was code, not data (philosophy #1)** — added `plan_store.py`: canonical plan in `~/.git-suite/plan.json`, seeded from plan.py; refactored hubs.py + scan.py to read it at request time; added GET/POST `/api/plan` + `/api/plan/verdict` + `/api/plan/reset` *(resolved 2026-06-02)*
- [x] **No reconciliation engine (philosophy #2)** — added `routers/reconcile.py`: `/api/reconcile/{session}` diffs latest scan vs plan → per-repo verdict, orphans, ghosts, hub + layer rollups, done-flags. Validated against live 118-repo scan *(resolved 2026-06-02)*
- [x] **No triage workflow (philosophy #3)** — added `/triage` SvelteKit page: keyboard-fast verdict queue (1–8 absorb, a/k/o/s), optimistic updates; added to nav *(resolved 2026-06-02)*
- [x] **layer-audit page was a stub** — `byLayer` always rendered empty; now populated from reconcile layer rollup, with hub labels per layer *(resolved 2026-06-02)*
- [x] **summary page read non-existent `hub._status` + undefined `orphans`** — rewritten on the single reconcile call; shows live/absorbed/archived/undecided/ghost stats, per-hub progress, next-action list *(resolved 2026-06-02)*

- [x] **place-time cross-layer conflict** — resolved: `place-time` is ontology-first (temporal/spatial as classification), not a mapping tool; removed from `map-suite` absorbs, kept in `ontology-align` only *(resolved 2026-05-24)*

- [x] **Path bug in embed_vba.ps1** — script deleted (defunct pipeline removed) *(resolved 2026-05-24)*
- [x] **compress variable dead in inject_datamashup()** — script deleted (defunct pipeline removed) *(resolved 2026-05-24)*
- [x] **Dead variable WithFullName in View_RepoFull M query** — script deleted (defunct pipeline removed) *(resolved 2026-05-24)*
- [x] **MarvelGraph duplicated in REPO_META** — duplicate entry removed *(resolved 2026-05-24)*
- [x] **Local-only repo whitelist duplicated** — consolidated to single `LOCAL_ONLY` set at top of file *(resolved 2026-05-24)*
- [x] **Missing pipeline step: xlsm origin undocumented** — entire Power Query pipeline removed *(resolved 2026-05-24)*
- [x] **Conflicting final outputs** — both competing pipelines removed; one script, one output *(resolved 2026-05-24)*
- [x] **No README / pipeline documentation** — replaced by `--help` text and inline usage comment *(resolved 2026-05-24)*
- [x] **All file paths hardcoded to H:\GitHub\\** — both scripts now use `Path(__file__).parent`; outputs live in the project folder *(resolved 2026-05-24)*

